
@{
    ViewBag.Title = "Categoría";
    ViewBag.SubTitle = "Listado de Categorías";
    ViewBag.Breadcrumbs = new[] {
        new BreadcrumbViewData { Name = ViewBag.Title }
    };
}

<div>
    <div class="animated fadeIn">
        <div class="card">
            <div class="card-header">
                <i class="fa fa-list"></i> @ViewBag.SubTitle
                <div class="card-header-actions">
                    <button class="btn btn-primary" data-toggle="modal" data-target="#add_modal">
                        <i class="fa fa-plus"></i>
                        Nuevo
                    </button>
                </div>
            </div>
            <div class="card-body">
                <div class="row mb-4">
                    <div class="col-md-2">
                        <label class="label">Disciplina</label>
                        <select id="discipline_filter" class="select2 select2-disciplines">
                            <option>Todas</option>
                        </select>
                    </div>
                </div>
                <div class="dataTables_wrapper dt-bootstrap4 no-footer">
                    <div class="row">
                        <div class="col-sm-12">
                            <table class="table table-hover table-striped table-bordered datatable dataTable no-footer" id="category_datatable" role="grid"></table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_CreateModal")
@Html.Partial("_EditModal")

@section Scripts {
    <script src="@Url.Content("~/Content/js/areas/admin/category/index.js")"></script>
}